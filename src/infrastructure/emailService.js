// src/infrastructure/emailService.js
class EmailService {
    async sendWelcomeEmail(email) {
      // Code to send the welcome email.
      console.log(`Sending welcome email to ${email}`);
    }
  }
  
  module.exports = { EmailService };
  